<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.luying.weixinSystem.mapper.SysPermissionMapper">
    <select id="queryOne" parameterType="java.util.HashMap" resultType="java.util.HashMap">
        select id,name,url from sys_permission where id = #{id}
    </select>
    <select id="queryList" parameterType="java.util.HashMap" resultType="java.util.HashMap">
        select id,name,url from sys_permission
    </select>
</mapper>